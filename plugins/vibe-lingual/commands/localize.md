---
description: Run the mutating localization loop — extract -> wire -> translate -> guard — with confidence routing, per-file backups, and idempotent re-runs. Mutates the target (confidence-routed).
---

Invoke the `vibe-lingual:localize` skill.
