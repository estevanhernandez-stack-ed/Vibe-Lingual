import React from 'react';

// Clean App Router home page — one un-extracted heading, no blocker.

export default function HomePage() {
  return (
    <main>
      <h1>Welcome to the dashboard</h1>
    </main>
  );
}
